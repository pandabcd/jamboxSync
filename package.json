{
  "name": "jambox",
  "version": "1.0.0",
  "description": "Syncs. youtube videos in different computers on the same network",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "subhodeep",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.3",
    "socket.io": "^2.0.3",
    "tcp-ping": "^0.1.1"
  },
  "devDependencies": {
    "nodemon": "^1.11.0"
  }
}
